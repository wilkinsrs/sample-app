<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %> <!-- automatically knows to look in the app/assets/views/ directory -->
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container"> <!-- container, nav, navbar-nav etc. are all special classes that have distinct meaning in Bootstrap -->
      <%= yield %>  
    </div>
    <%= render 'layouts/footer' %>
    <%= debug(params) if Rails.env.development? %> <!-- this adds a small debugging UI to the UI -->
  </body>
</html>